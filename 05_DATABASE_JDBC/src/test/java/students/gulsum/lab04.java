package src.test.java.students.gulsum;

public class lab04 {
    //select distinct salary from employees where rownum <=3 order by salary desc;
    //select min(salary) from (select distinct salary from employees  order by salary desc) where rownum <6
   // select distinct salary from employees where rownum <6  order by salary desc;
    //
}
